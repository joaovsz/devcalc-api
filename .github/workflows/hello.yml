name: Hello Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Mensagem inicial
        run: echo "Pipeline iniciado com sucesso"
